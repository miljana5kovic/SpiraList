import { EventEmitter, Injectable, Output } from "@angular/core";

@Injectable({
    providedIn: 'root'
})
export class SortAlgoService {
    bubbleSort(animation: boolean): void {
        
    }

    insertionSort(animation: boolean): void {

    }
    
    selectionSort(animation: boolean): void {

    }
}